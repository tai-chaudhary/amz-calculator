// Bump on every release. Minor (x.Y) for new features, patch (x.y.Z) for fixes.
// Record what changed in CHANGELOG.md at the project root.
export const VERSION = '2.6.0'
// eslint-disable-next-line no-undef
export const BUILD_TIME = typeof __BUILD_TIME__ !== 'undefined' ? __BUILD_TIME__ : null
